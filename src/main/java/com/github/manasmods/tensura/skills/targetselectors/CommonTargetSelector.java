package com.github.manasmods.tensura.skills.targetselectors;

/**
 * This is a common target selector and can be used on client and server
 * Common target selectors should be usually used on the server as their effect would be limited on client-side requiring the use of a {@link PacketTargetSelector} on server side
 */
public @interface CommonTargetSelector {
}
